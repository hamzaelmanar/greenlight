name: greenlight_transform
version: "1.0.0"
config-version: 2

profile: greenlight_transform

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  greenlight_transform:
    staging:
      +materialized: view
    marts:
      +materialized: table
